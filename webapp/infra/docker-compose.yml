services:
  app:
    build:
      context: ..
      dockerfile: infra/Dockerfile
    ports:
      - '8067:80'
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost/']
      interval: 30s
      timeout: 3s
      start_period: 5s
      retries: 3
