services:
  app:
    build:
      context: ..
      dockerfile: infra/Dockerfile
    expose:
      - "80"
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost/']
      interval: 30s
      timeout: 3s
      start_period: 5s
      retries: 3
